{% set name = "intake-axds" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/intake_axds-{{ version }}.tar.gz
  sha256: f7091f9809e9f154583924ab1631f3429eb85006912d07d94ea2eaccc0ccfe4d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools-scm
  run:
    - cf_pandas
    - intake
    - intake-parquet
    - intake-xarray
    - pandas
    - python >=3.6
    - requests
    - shapely

test:
  imports:
    - intake_axds
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/axiom-data-science/intake-axds
  summary: Intake interface to Axiom assets
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - kthyng
